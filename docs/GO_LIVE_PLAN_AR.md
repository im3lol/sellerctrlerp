# خطة الإطلاق — SellerCtrl

**الهدف:** إطلاق تجاري لبائعي Amazon (نون وWoo مقفولين في الإنتاج لحد قرار تاني) على نظام بيقوم لوحده بعد أي عطل، بيانات كل شركة معزولة،
وحساباته ومخزونه متطابقين.
**قاعدة القرار:** مفيش إطلاق قبل ما كل بنود المرحلة 0 و1 تتقفل، والـrehearsal النهائي (المرحلة 6) ينجح.

**الرموز:** ✅ موجود — المطلوب تحقق وتوثيق بس · 🔧 محتاج شغل · 👤 مستني قرار أو بيانات من صاحب السيستم

> **حقيقة لازم تبقى قدامنا:** الإنتاج جهاز واحد (Docker على الجهاز ده + نفق Cloudflare). أي ريستارت أو
> انقطاع كهربا = السيستم واقع. يوم 2026-09-16 وقع ~9 ساعات لأن Docker ماقامش بعد الريستارت، ومحدش اتنبّه.

---

## المرحلة 0 — استقرار التشغيل (فورًا، قبل أي حاجة)

| # | البند | الحالة | معيار الإغلاق |
|---|---|---|---|
| 0.1 | تشغيل Docker Desktop تلقائيًا مع تسجيل الدخول (`AutoStart: true` ✅) + تسجيل دخول تلقائي للويندوز (`netplwiz`) | 👤 | ريستارت فعلي للجهاز → الموقع يرجع 200 من برّه بدون تدخل |
| 0.2 | الـwatchdog بينبّه لما التطبيق نفسه يقع كمان، وبيشغّل Docker لو واقف | ✅ | إيقاف Docker عمدًا → رسالة تليجرام خلال 10 دقايق، ورسالة رجوع بعد التشغيل |
| 0.3 | مراقبة من **خارج الجهاز** (UptimeRobot / Healthchecks.io على `https://app.sellerctrl.com/api/health`) — لأن لو الجهاز نفسه وقع أو النت قطع، مفيش سكربت محلي هيبعت حاجة | 👤 🔧 | فصل النت عن الجهاز → تنبيه يوصل |
| 0.4 | UPS للجهاز والراوتر، وضبط الويندوز إنه مايعملش ريستارت تحديثات في وقت الشغل | 👤 | — |

## المرحلة 1 — بوابة الإطلاق (P0)

| # | البند | الحالة | ملاحظات / معيار الإغلاق |
|---|---|---|---|
| 1.1 | كلمات سر الإنتاج (الداتابيز و`appuser` وMinIO) | ✅ | اتغيّرت (كودكس) وCompose و`lib/env.ts` و`deploy.sh` بيرفضوا القيم الناقصة؛ الباك أب وMinIO اتأكدوا بعدها. Adminer على `127.0.0.1` بس |
| 1.2 | تدوير الأسرار اللي اتكشفت قبل كده (`docs/SECRET-ROTATION.md` موجود) | ✅ دليل · 🔧 تنفيذ | كل سر في الدليل اتغيّر، والقديم اتلغى عند المزوّد |
| 1.3 | **SMTP** — من غيره مفيش إيميل تسجيل ولا استرجاع باسورد ولا تذكيرات المتأخرات ولا الإيميل اليومي | 👤 | إيميل تجربة يوصل من `/admin/integrations` |
| 1.4 | قرار التسجيل | ✅ | مفتوح للكل (`SIGNUP_OPEN=1` في `.env`)، والتجربة 30 يوم (`lib/erp/trial.ts`) |
| 1.5 | تتبّع الأخطاء (Sentry DSN) — الـlogger جاهز ومستني المفتاح | 👤 | خطأ تجريبي يظهر في Sentry |
| 1.6 | smoke test قبل وبعد كل نشر، كسكربت مش كقائمة يدوية: health، تسجيل دخول، صلاحيات، عزل شركتين، إنشاء وتأكيد مستند | 🔧 | السكربت بيتنادى من `scripts/deploy.sh` بعد الـhealth gate، والفشل = rollback |

## المرحلة 2 — صدق صفحة الهبوط والتحويل

| # | البند | الحالة | ملاحظات |
|---|---|---|---|
| 2.1 | robots / sitemap / OG / metadata | ✅ كود | `MARKETING_URL` هو الأصل التسويقي (`sellerctrl.com`) و`APP_URL` يظل للتطبيق. التحويل 308 يوحّد `/` وsignup/privacy وrobots/sitemap من `app` و`www` للأصل؛ `/d/` ممنوع من الفهرسة. صورة المشاركة مقاسها الحقيقي 1536×1024 |
| 2.2 | «بيانات توضيحية» تحت صورة لوحة التحكم | 🔧 (مسودة كودكس) | — |
| 2.3 | طريقة تقييم المخزون | ✅ محسومة | المحرك **FIFO بالدفعات** (`lib/erp/inventory.ts`) + FEFO للصلاحية. جملة «(FIFO)» اللي كودكس شالها **صحيحة** وترجع |
| 2.4 | مراجعة كل ادعاء في الصفحة قصاد المنتج | ✅ | نون «قريبًا» في كل مكان، والتجربة 30 يوم |
| 2.5 | الأسعار والـCTA حسب قرار 1.4 | 👤 🔧 | CTA أساسي واحد |
| 2.6 | موبايل + كيبورد وتباين | ✅ جزئيًا | الهبوط والتسجيل والدخول على 390px من غير scroll أفقي، والحقول متربطة بعناوينها. صفحات جوّه النظام محتاجة فحص وانت مسجّل دخول |

## المرحلة 3 — أمان SaaS متعدد الشركات

| # | البند | الحالة | ملاحظات |
|---|---|---|---|
| 3.1 | عزل الشركات (RLS) | ✅ | التطبيق بيتصل بـ`appuser` (مايقدرش يتخطّى RLS)، وRLS مفروض على 152 جدول. `scripts/checks/chk-idor-all-tables.ts` بيثبت إن مفيش صف من شركة بيبان لشركة تانية، على كل الجداول |
| 3.2 | حدّ المحاولات للتسجيل والدخول | ✅ | على Redis (بيصمد مع الريستارت والنشر)، ولو Redis واقع بيرجع للذاكرة. وفوقه قفل لكل حساب في الداتابيز |
| 3.3 | إلغاء مفاتيح الـAPI | ✅ | `revokeApiKeyAction` موجودة |
| 3.4 | «اخرج من كل الأجهزة» | ✅ | `users.session_version` جوّه كل JWT. الزرار في «الأمان»، وتغيير الأدمن لباسورد حد بيخرجه من كل الأجهزة |
| 3.5 | اختبارات IDOR | ✅ | فحص على مستوى الداتابيز لكل الجداول (فوق). المسارات كلها بتقرا بـ`withOrgScope`، و`rls:check` في CI |
| 3.6 | صلاحيات المالك والدعم والأدمن | ✅ | `docs/ROLES_AR.md` |
| 3.7 | خطة الحوادث | ✅ `docs/incident-response.md` | تجربة فعلية مرة (tabletop) وتحديث الأسماء والتليفونات |
| 3.8 | حذف شركة بالكامل | ✅ | المالك يطلب من «بيانات المنشأة» بكتابة الاسم، ومهلة 14 يوم يقدر يلغي فيها، وبعدها الـcron بيمسح. الشركات التجريبية المنتهية بتتمسح بنفس الخطوة |

## المرحلة 4 — Amazon ونون والموثوقية المالية

| # | البند | الحالة | ملاحظات |
|---|---|---|---|
| 4.1 | شاشة «صحة أمازون» واحدة | ✅ | `/platforms/amazon/health` |
| 4.2 | سيناريوهات end-to-end | ✅ جزئيًا | `scripts/checks/chk-amazon-recon.ts`: توازن الدفتر، 1104 = تقييم المخزون، كل طلب مفوتر ليه قيد وتسليم، التسويات ↔ القيود/الطلبات/المرتجعات. «كوميتو»: كل الفحوص الأساسية ناجحة. سيناريوهات retry وفروق العملة لسه |
| 4.3 | runbooks | ✅ | `docs/runbooks/` — النفق، الـworker والطوابير، OAuth أمازون، الباك أب |
| 4.4 | نون وWoo | ⏸ | مقفولين في الإنتاج بقرار المالك (أمازون بس حاليًا)؛ الـwebhooks بترجّع 410 |

## المرحلة 5 — التشغيل والاستعادة

| # | البند | الحالة | ملاحظات |
|---|---|---|---|
| 5.1 | `/api/health` (داتابيز + Redis + نبض الـworker) | ✅ | — |
| 5.2 | الرجوع التلقائي لو النشر طلع مش سليم | ✅ في `deploy.sh` | تجربة rollback مقصودة وقياس الوقت |
| 5.3 | الباك أب: نسخة لكل شركة + `pg_dump` كامل | ✅ | `scripts/restore-drill.sh` اتجرّب 2026-09-18: 10/10 جداول متطابقة، الاستعادة 11 ثانية. المرفقات لسه ماتجرّبتش |
| 5.4 | نسخة احتياطية **خارج الجهاز** (R2 / S3) | 👤 | أداة الرفع `mc` اتصلّحت (كان رابطها بيرجّع 410) وفيه تنبيه لو غابت. ناقص بس مفاتيح `OFFSITE_S3_*` |
| 5.5 | تنبيهات: تراكم الطوابير، فشل مزامنة أمازون، فشل الباك أب | 🔧 | كل تنبيه ليه مسؤول وrunbook |
| 5.6 | البناء بقى بيبدأ من غير كاش كل مرة (كان بيعلّق) | ✅ | — |

## المرحلة 6 — Rehearsal الإطلاق

ريستارت للجهاز ← نشر ← smoke test ← rollback مقصود ← استعادة باك أب ← فصل النت وتأكيد وصول التنبيه.
**معيار الإغلاق:** كل خطوة نجحت، متوثّقة بالوقت اللي خدته.

## بعد الإطلاق (مش بتوقفه)

- الفاتورة الإلكترونية **ETA** 👤 — مؤجلة. محتاجة قرار طريقة التوقيع، ومهمة لعملاء الشركات في مصر.
- بوابة الدفع أونلاين 👤 — المكان جاهز في رابط العميل.
- قراءة الفواتير بالذكاء الاصطناعي 👤 — مستنية مفتاح وموديل من لوحة الأدمن.
- توحيد الأرقام على اللاتينية (القاعدة الحالية `ar-EG-u-nu-latn`) + فحص في CI للنصوص الثابتة.

---

## ترتيب التنفيذ

1. المرحلة 0 (اليوم).
2. المرحلة 1 — 1.4 و1.3 قرارات، و1.1 في نافذة صيانة، وبعدها دمج تعديلات كودكس المصلّحة.
3. المرحلة 2 و5 بالتوازي.
4. المرحلة 3 ثم 4.
5. المرحلة 6، وبعدها الإطلاق.
